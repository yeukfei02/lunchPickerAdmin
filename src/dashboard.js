import React from "react";
import { Card, CardContent, CardHeader } from "@material-ui/core";

function Dashboard() {
  return (
    <Card>
      <CardHeader title="Welcome to Lunch Picker Admin Portal" />
      <CardContent>Lunch Picker Admin Portal</CardContent>
    </Card>
  );
}

export default Dashboard;
